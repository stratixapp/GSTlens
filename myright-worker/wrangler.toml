name = "myright-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Uncomment and fill this for custom domain:
# routes = [
#   { pattern = "api.myright.app/*", zone_name = "myright.app" }
# ]

[vars]
ALLOWED_ORIGIN = "https://my-right-a4982.web.app"

# GEMINI_API_KEY must be added as a Secret (not here):
# Run: wrangler secret put GEMINI_API_KEY
# Then paste your key when prompted
